packages:
    default:
        actions:
            helloJS:
                code: |
                    function main(args) {
                        const name = args && args.name || "stranger";
                        return { greeting: `Hello ${name}!` };
                    }
                runtime: nodejs:10
